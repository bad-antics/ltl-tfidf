// examples/demo.ltl — runnable demo for ltl_tfidf

import std::io
use ltl_tfidf as lib

fn main() {
    io::println("ltl_tfidf demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}