// ltl-tfidf/main.ltl
//
// TF-IDF vectoriser + cosine nearest-neighbour search.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-tfidf v0.1.0")
    io::println("TF-IDF vectoriser + cosine nearest-neighbour search.")
    0
}
